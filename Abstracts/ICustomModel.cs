namespace BaseLib.Abstracts;

/// <summary>
/// Checked in PrefixIdPatch to determine if a prefix should be added and occasionally to check if a model is
/// using BaseLib.
/// </summary>
public interface ICustomModel;